#include "main.h"
/**
 * set_string - replace with the value for array to
 *
 * @s: arrary of s that we need to replace
 * @to: array of char that we will take
 * Return: void
 */
void set_string(char **s, char *to)
{
	*s = to;
}
